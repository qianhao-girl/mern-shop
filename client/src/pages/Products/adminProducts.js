import React, { Component } from 'react'

export default class adminProducts extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                adminProducts page
            </div>
        )
    }
}
